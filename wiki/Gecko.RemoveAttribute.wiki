RemoveAttribute will remove an attribute from the element encapsulated by the [Gecko.DOMNode DOMNode]. If the node is not an element, it will return false.

==Parameters==
|| *Name* || *Type* || *Description* ||
|| Name || String || The name of the attribute to remove ||