ExecQuery will execute a given WQL query against a WMI namespace and return an enumerator. On failure, it will return false.

==Parameters==
|| *Name* || *Type* || *Description* ||
| Query || String || The query to execute against the namespace ||