EnumValues will enumerate the values of a key into an array. The function takes no parameters and will return an array object on success and false on failure. More information about an error can be retrieved from [GetLastError]. The key must be opened with KEY_ENUM_VALUES in order to make this call.